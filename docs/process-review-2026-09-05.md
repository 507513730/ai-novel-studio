# 流程与文档审查（2026-09-05）

## 结论

有实际问题，不只是文案过时。发布状态误报、门禁顺序、证据归属和互相冲突的操作指引都可能导致错误交付。本轮仅修流程、CI、测试工具与活跃文档，不新增业务功能。

| 优先级 | 已确认问题 | 本轮处理 |
|---|---|---|
| 高 | v1.1.0 标记已发布，但只有失败 tag，没有 Release | 对比 48 个 Release，纠正唯一不匹配行；保留 run 33290490484 |
| 高 | --push 的完整 E2E 可漏跑，冒烟放在发布之后 | 发布前强制 backup/smoke/full 门禁，失败即停 |
| 高 | 最新一次 CI 可能属于其他 SHA；某个平台先完成就先发布 | 按 SHA/分支/事件/工作流筛选；整个矩阵成功后单一 publish job |
| 高 | 退出码 0、PASS 字样、部分轮次或旧报告可误当通过 | 完整 T1-T5、明确完成状态、SHA/版本/provider/app.asar 哈希、24 小时有效期共同校验 |
| 高 | 文档指导删失败 run、删 tag 重打、明文 Key 测试 | 活跃指南移除；文档守护拦截这些旧操作命令 |
| 中 | --bump 修改版本后又按新版本找旧锚点，制造重复标题；容易反复 bump | 单独准备步骤，未打 tag 的候选禁止再 bump；解析失败不写文件 |
| 中 | shared 根目录不在源码检查范围；git diff 失败被吞 | 修正路径并让 diff 错误阻断 |
| 中 | 已实现备份变更留在 Unreleased，却已打入 v1.1.1 候选包 | 合并进实际候选版本说明，不制造额外空版本 |
| 中 | 活跃开发/打包手册仍调用旧固定端口、明文验收脚本 | 改为独立 Electron/utilityProcess、随机端口和加密凭证 |
| 中 | 本地模块被 release/ 忽略规则误排除，可能本地绿而 CI 缺文件 | 发布契约采用 scripts/release-contracts.mjs，未强制绕过 ignore |
| 中 | 源码推送与正式发布混淆，导致没有远端 CI 却持续等待 CI | 分离本地门禁→源码 push→指定 SHA CI→正式 tag/Release |

## 方向重做 500 的定位

同样两个接口、同样默认 flash 模型：网关首次方向生成成功（42149ms），重做记录 upstream-timeout 后返回 500；官方备用初次 10017ms、重做 15440ms 均成功。此次已确认走到上游超时失败路径，没有依据归咎于本地 JSON 替换算法。

对照仅是定向诊断，不是完整生产验证；脏工作区证据不会被发布脚本接受。完整 E2E 必须另跑，实际 provider 进入证据。界面通用 500 的可诊断性仍可后续改善，本轮未扩大到业务错误模型重构。

## 仍需用户裁定的硬约束文字

本轮没有修改 AGENTS.md 的硬约束。建议单独确认后同步：

1. #36 固定 0.1.0 文件名与当前 package.json/artifactName 不符；建议采用实际版本模板。
2. #48 的“复制 db 三件套”与已验收的一致性快照实现不符；建议明确一致性快照 + 完成清单 + 进行中保护是当前标准，不能恢复活跃文件拼接方案。
3. #60b 的本地门禁后推送与 #62 的“只过本地三绿不能 push”存在字面循环；建议统一为本地门禁→push→同 SHA CI→发布，保留所有质量门禁。
4. #51 禁止重指 tag 与 #62 历史提交重放后重指 tag 的例子冲突；建议历史事故处置不构成日常授权，默认始终不重指已发布 tag。

当前执行采用更保守的权限/数据保护边界：不改历史 tag，不删失败 run，不写真实用户库，不绕过失败 E2E。

## 执行复盘

前两轮的“先扩展后收尾”、重复打包、没有提交绑定的证据，以及没有及时核对远端发布状态，都已造成不必要成本。这些是执行流程问题。本轮新增流程回归测试与文档断言，不只追加一段“以后注意”。

实际新一轮本地门禁、CI 与正式发布结果应分别记录；本文件不是“全仓审查已完成”的声明。其余业务域与任意磁盘故障下的恢复回滚仍在原审查清单中。

## 本地验证记录（本轮提交前）

- typecheck 通过；lint 0 error / 5 条既有 warning。
- 69 个文件、447 条测试全部通过；机器报告：release/process-review-tests.json。
- db-smoke 7/7；生产依赖审计无 high/critical，仍有 2 moderate。
- pnpm 10.34.5 frozen-lockfile 安装通过；6 个 workflow YAML 解析通过；文档健康、版本唯一性/安全指引与远端发布台账校验通过。
- 尚未将这些本地结果称为远端 CI 或正式发布证据；下一阶段先推送当前 SHA，再验证该 SHA 的 CI 与打包候选。

## 构建依赖审计补充（D131）

源码 push 后核对到 4 条 high，均来自开发工具链 fast-uri 3.1.5。此前 prod 审计结果没有错误，但不能扩张为全依赖无高危。现限定修补到官方 3.1.6，并把全依赖审计纳入本地/CI；剩余中危单独披露。

### D131 修补后验证

- 全量 69 个文件 / 448 条测试通过，类型/lint/文档检查通过。
- 全依赖 audit：0 high/critical，3 moderate；prod audit：0 high/critical，2 moderate。
- 锁文件包版本集合仅 fast-uri 3.1.5 → 3.1.6；其他变化为 pnpm 平台元数据与单一 overrides 配置规范化，不升级其他包版本。
- 安全/流程补充不新增应用版本，仍属尚未发布的 v1.1.1 候选。

## 截图门禁补充（D132）

最前置的 backup 因辅助截图 UnknownVizError 停止，尚未付费跑完整套件。复查发现旧截图可能是加载占位或旧绘制帧；现加入 DOM 就绪、绘制帧同步、纯背景拒绝与三次有限重试，最终失败仍阻断。实际图片已核对为首启向导，真实备份恢复重跑通过。这是测试证据完善，不宣称所有页面视觉已验收。

### D132 验证记录

- 原配置全量复跑 69 个文件 / 456 条测试通过；此前 worker 通道关闭的失败记录保留，不使用忽略异常选项。
- DOM 就绪、两帧同步后的截图已实际核对为首启向导，非加载占位或纯背景；独立备份恢复闭环通过。
- 本轮仍在发布候选阶段，最终是否发布只看后续绑定提交的门禁及 GitHub 结果。

## 测试进程预算补充（D133）

原配置再次触发 worker IPC 关闭，发布按设计停止。固定 4 个独立 worker 的两轮对照均通过全部 456 用例，因此将并发预算显式固化；不删除测试、不忽略未处理异常，也不声称已确定底层故障机理。

固化配置后的默认入口再次通过 69 文件 / 457 用例；类型、lint 和文档门禁通过。后续发布仍会从该配置重新验证，不借用临时 CLI 覆盖的结果。

## 后续反证与修正（D134/D135）

完整 T1-T5 首次实际通过 67 项，但本 agent 新增的结果转发层漏传两个已采集字段，发布被阻断；现统一对象序列化并用真实文件往返验证，每个 gate 立即校验证据。不会修改旧报告来绕过失败。

四个 forks 仍复现通道关闭，不能继续声称预算限制已解决；隔离 threads 池两轮各通过 460 用例，现保留 isolate=true 和全部异常判定替换测试传输。原应用独立 utilityProcess 不变。

结果协议与隔离 threads 默认入口最终通过 69 文件 / 460 用例，类型/lint/文档门禁通过；真实父进程证据已核对 rendererReady=true、captureAttempts=1，未修改旧证据。

## v1.1.1 正式发布结果（2026-09-05）

- tag：v1.1.1，SHA：59b5671b6335fe1b035c7428301b5828db3b22c5。
- 对应 Build Release：33958310816，成功；main 的 Build Release/CodeQL/Release Readiness/Docs Lint 也均成功。
- GitHub Release：已发布，publishedAt=2026-09-05T09:38:09Z，非 draft、非 prerelease。
- 资产已核对：Windows NSIS/portable、latest.yml、macOS DMG/latest-mac.yml、Linux AppImage/latest-linux.yml 及 blockmap。
- 发布流程此前因 GitHub API EOF 停止等待，但只读重试确认构建与 Release 已成功；未删除、重打或复用 tag。
- v1.1.1 现在才可称为正式发布；更早记录中的“待发布/候选”均为发布前快照。

## 前端批次补充（D137）

- 修复 4 条既有 lint warning：Fast Refresh 导出拆分 3 条、solutionAssets 未使用禁用指令 1 条、WritingPanel effect 依赖 1 条；本批 lint 变为 0 warning。
- 写作偏好异步状态抽为 useWritingSettings，真实 PATCH 共享锁与 UI disabled，失败/加载可重试，生命周期序号拒绝过期响应。
- 相关 81 条测试通过；全量 69 文件 / 460 用例通过；dist 产物已按 v1.1.1 重建。
- 目前未拆大页面；大页面重构留给独立批次，防止与状态竞态修复混合。
