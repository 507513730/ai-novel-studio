# 全仓审查与重构进度（2026-09-05）

## 范围与状态

用户已批准全仓分批审查。此记录为**第一批安全边界修复与全仓初筛**，不代表逐文件深审或全仓重构完成。未操作真实用户数据库，未创建 tag 或发布 Release。

- 盘点：client 92、server 104、electron 9、shared 1 个 TypeScript 源文件；66 个测试文件。
- 基线：typecheck 通过；lint 0 error / 5 warning；66 文件 / 385 用例通过。
- 本批改动：electron/ipc.ts、electron/window.ts、tests/electron-security-contract.test.ts；机制查证见 decision-log D127。

## 已修复

### S1：IPC 校验混淆顶层祖先与发送者（高优先级）

原 isTrustedSender 检查 senderFrame.top === mainFrame。子 frame 的 top 同样指向主 frame，不能证明发送者本身是顶层；缺失 senderFrame 也被放行。改为主窗口 webContents 与 senderFrame === mainFrame 双重校验，窗口尚未注册时 token 通道同样 fail-closed。window.ts 在加载页面前注册主窗口，不依赖启动期豁免。

测试补全同窗口父子关系、缺失/null frame、启动期 token 拒绝、合法主 frame 放行。该问题是校验逻辑缺陷；本次没有宣称已完成实际 XSS 到 IPC 的可利用链验证。

### S2：导航白名单使用字符串前缀（高优先级）

原开发态 startsWith(devUrl) 会放行端口前缀伪装或 userinfo URL；原打包态放行任意 file URL，不限本应用页面。现在开发态比较 HTTP(S) origin，拒绝用户名/密码；打包态只允许实际 renderer/index.html，保留 hash/search。非法 URL fail-closed。

## 待处理发现（尚未修复）

1. **备份导出递归删除用户所选现有目录**：electron/ipc.ts 的 export-backup 在 existsSync(outDir) 后 rmSync(outDir, recursive)，没有验证目录是否只包含本应用备份。应禁止覆盖非备份目录，拒绝数据目录及其祖先；在临时目录构造哨兵文件回归，不能拿真实目录做实验。
2. **checkpoint 失败被当成成功**：electron/shutdown.ts 的 requestCheckpoint 在 timeout、checkpoint-error、无服务时均 resolve；调用方继续复制主库。server/src/index.ts 的 checkpoint 应答也未检查 pragma 返回状态。下一批需设计可证明一致的备份快照协议，加入错误、超时、并发写入测试，不只修注释或延长等待。
3. **写作偏好缺少操作防重**：client/src/pages/settings/WritingPanel.tsx 的 patch 没有 busy 守卫，重叠请求可能让本地显示与数据库最终值不一致；需串行化或明确 latest-write 语义并测试乱序完成。
4. **组件超限**：StudioPage.tsx 727 行；ChapterExecutionPage.tsx 425 行；VolumePanel.tsx 402 行；StylePanel.tsx 14 个 useState（粗略文本统计，拆分前按组件职责复核）。按纪律先抽 hook，再拆 UI，不继续叠加功能。
5. **模型调用后续深审**：caller.ts 的流式输出在已发 delta 后失败仍可能换候选，需核对 onDelta 消费者是否清空上一尝试；此项只是风险线索，尚未完成复现。
6. **依赖与文档不一致**：package.json 的 Electron 为 43.3.0，用户约束为 43.4.1。官方发布页本次请求发生 EOF，未确认可用性，未擅自变更依赖。生产 audit 为 0 high/critical，仍有 2 moderate，不能写成零漏洞。
7. **E2E 隔离门禁**：scripts/e2e/common.mjs 固定 http://127.0.0.1:3000/api；不能直接运行，以免碰到真实开发实例。正式发版前必须建立独立测试 UDATA，确认测试服务来源，再执行 T1-T5。

## 后续批次

- 安全与数据完整性：备份/恢复/关闭协议故障注入及 Electron 版本核对。
- 核心生产链：job/章节 claim、取消与 kill 后恢复不重复的实进程验证。
- 模型与上下文：流式重试边界、fallback、JSON 截断、前缀冻结与用量核对。
- 前端：保存竞态、防重和超限组件拆分。
- 工程：CI 全套、E2E、安装包冒烟与发布闭环。

## 发布边界

源代码修改需要 PATCH 版本与 CI release-readiness 同批闭环。用户批准了审查/重构，但此前计划明确发布另行批准；本批不擅自 bump/tag/push Release，也不将版本仍为 1.1.0 的本地重建包冒充新正式版本。发布授权及剩余门禁未完成前，保持本地待发布状态，不推未经发布一致性验证的源代码。

## 首批验证结果

- typecheck 通过；lint 0 error / 5 条既有 warning。
- 全量 vitest 通过（66 文件；在原 385 用例上新增 9 条，共 394 条）；安全契约单独复核 18/18。
- pnpm dist 成功：NSIS 131115899 字节（2026-09-05 13:29:31），portable 130822105 字节（2026-09-05 13:29:38），时间为本机 Asia/Shanghai。两个产物仍是 1.1.0 本地重建，不是新发布版本。
- pnpm audit --prod --audit-level=high 通过：2 moderate、0 high/critical。
- verify-docs 与 git diff --check 通过。
- 本批未更改数据层，未执行 db-smoke；未运行 T1-T5、真实 kill 恢复、安装包运行/安装冒烟或远程 CI。全仓重构与正式交付尚未完成。
- 未提交、未推送：待发布授权与版本一致性门禁闭环，不向 main 推送明知缺少版本 bump 的源代码。

## 发布授权与准备（2026-09-05）

用户已授权 v1.1.1 发布并继续下一批。release 脚本已 bump 到 1.1.1；Electron 43.4.1 已经官方 npm registry 确认并精确锁定。新增 E2E 隔离与失败退出契约测试 3 条。此前“未获发布授权”描述仅为首轮历史状态；是否完成发布以随后验收记录为准。

### v1.1.1 本地候选验证

- Electron 43.4.1 构建成功；NSIS 135109056 字节（13:45:09），portable 134815141 字节（13:45:14），日期均为 2026-09-05。
- app.asar 独立 Electron 等价冒烟通过：真实 preload token、无 token 403、加密导入凭证、SSE 生成与 TXT 导出，结果目录 release/release-e2e-KD9FEA。
- 完整 T1-T5 正在独立实例执行，尚不能判定发布门禁完成；不把前一轮主动停止的测试记作通过。

## 发布门禁结果与下一批（2026-09-05）

### 首批未发布

- 候选代码已本地提交为 51b05c8，pnpm release（不带 --push）的本地完整流程通过；未推 main、未打 tag、未创建 Release。
- 真实 T1 为 10/10；T2 的定向重做方向返回 HTTP 500，两个关联断言失败。后续观察到 framing、宏观、世界观、角色、卷、节拍板与章节清单生成通过，但这不抵消失败。
- 为避免继续无效计费，已主动终止该失败轮次并保留证据：release/desktop-e2e-1788587412415.log 与 release/release-e2e-0sRTk5。该轮没有完成 T1-T5，不能写成全轮通过。
- 旧运行中启动器的结束文案为 T1-T5 completed，但 result.code=4294967295 代表测试被终止；结束文案与 GUI 退出码不能当通过依据。当前启动器已改为显式非零退出 + 私有结果文件双重校验，并区分 failed/interrupted。
- 500 的根因尚未确认，不能直接归咎于网关；需要补采服务端诊断并定向复测，再补齐完整 E2E 与 CI 后才可发布。

### 下一批已实现（D129，本地候选）

- 抽出 electron/backup.ts：新目录独占预留、一致性快照、未完成标记、完成清单、受管轮转与恢复路径校验。
- server/src/services/utilityCommands.ts 统一消费 parentPort 的 event.data；使用 SQLite VACUUM INTO 生成独立快照，避免 checkpoint 与外部复制之间的写入竞态。
- 请求 UUID、失败/超时/退出拒绝；关闭命令先注册退出监听，并发关闭共享等待，kill 后仍等实际退出。
- 服务退出前显式 db.close，修复真实 Windows 恢复冒烟出现的旧 WAL EPERM；应用正常退出仍有异常兜底，不把异常关闭当作允许恢复的确认。
- 导出、恢复、清库与自动备份共享操作互斥，覆盖原生对话框等待期，防止交错替换/重启。
- 恢复不再根据“缺少 db 文件”猜测父目录；未完成或无效目录直接拒绝。
- 新增 16 条备份/协议/关闭测试与 1 条跨操作互斥测试；此前快速构建的真实备份→变更→恢复→数据校验已通过（release/release-e2e-yloLTq）。最终打包态结果见后续记录。

### 仍未完成

正式 v1.1.1 发布、完整付费 E2E、远端 CI，以及后续模型/前端等全仓批次。备份恢复在任意文件系统故障下的多文件事务回滚仍需进一步加固；本批验证了正常恢复、目标目录保护和关闭失败拒绝，不宣称覆盖所有磁盘故障。

## 下一批最终本地验收（2026-09-05）

- typecheck 通过；lint 0 error / 5 条既有 warning；68 个测试文件、414 条测试通过；db-smoke 通过。
- pnpm dist 完成：NSIS 135109637 字节（14:29:05），portable 134815719 字节（14:29:11），均为本地 1.1.1 候选包。
- 新 app.asar 真实备份/恢复闭环 PASS：快照前后哨兵校验、现有目录保护、服务退出与重启；结果见 release/release-e2e-6oC0jz/result.json（code=0）。测试不调用模型，不操作真实用户数据库。
- 文档健康检查、verify-docs 与 git diff --check 通过。
- 本批仍不推送或发布：首批完整 E2E 未过，远端 CI 尚未触发。发布授权保留，但必须先关闭方向重做 500 与完整 E2E 门禁。
