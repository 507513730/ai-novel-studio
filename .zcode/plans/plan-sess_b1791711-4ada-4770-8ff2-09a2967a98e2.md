# 全量源码严格审查 — 报告交付计划（桌面版）

## 交付物（按用户要求改到桌面）
- **新建 `C:\Users\Lenovo\Desktop\AI-Novel-Studio-审查报告-2026-08-13.md`**（不覆写仓库内 docs/audit-report.md，保持仓库不动）。
- **不改任何源码 / 配置 / package.json**；不跑 `pnpm dist`（用户选「仅出报告」）。
- 报告写完后做 grep 自检（核对严重度条数与正文一致等），确保文档内部一致（AGENTS.md #61b 文档断言纪律）。

## 审查方法（已完成）
- 3 个 Explore agent 并行，**通读全部 ~194 源文件**：①`server/src`（routes+services+db+prompts）；②`electron/`+`shared/`+`server/src/index.ts`+tsconfig/package.json/.npmrc/electron.vite.config.ts；③`client/src`（48 文件）。
- 四维度：正确性与 Bug / 安全 / 代码质量与可维护性 / 类型安全与约束合规。
- 已逐条复核 HIGH 级 file:line（见下「已复核」标记），MEDIUM/LOW 写入前抽样复核，不复现的标 ⚠️待核 或剔除。

## 报告结构
1. **概览**：范围、方法、文件计数、各严重度条数（CRITICAL 0 / HIGH 11 / MEDIUM ~30 / LOW ~25）。
2. **严重度分层总表**（一行一条，含 file:line）。
3. **按模块分组明细**：server / electron+shared+config / client。每条 = `file:line — 维度 — 描述 — 证据片段 — 修复建议`。
4. **AGENTS.md 硬约束违反索引**：映射 #6（safeStorage 明文两处）/ #9/#23（重启不重置 chapter generating）/ #19（before-quit 无 server.close）/ #20（snake_case 泄漏三处+shared 类型）/ #22（shared/types 注释乱码）/ #30（reserved model_route 死配置）/ #31（planner prompt 重复内联）。
5. **系统性问题**（跨文件模式）：state-而非-ref 的 busy 锁、snake_case 泄漏、prompt/parse 重复、死配置、CSP 缺失、window.confirm 与 useConfirm 并存。
6. **建议修复顺序**（仅参考，不执行）。

## HIGH 级已复核清单（写入报告的依据）
- `server/src/services/apiError.ts` ✅ — `ApiError`/`publicErrorText` 死代码（无 `new ApiError` 消费方）；中间件无 `instanceof ApiError` 分支 → `.status` 被吞，全走兜底 500。
- `server/src/services/generate.ts:44-51,117-126` ✅ — 原子抢占 `'generating'` 后无 `try/finally`；空内容 `if (content)` 跳过置 `'written'` → 章节永久卡 `'generating'`。
- `server/src/services/scheduler.ts:197-199` ✅ — 重启只重置 `job running→queued`，**不重置 `chapter 'generating'→'planned'`**。
- `server/src/services/hub.ts:147-160,340-352` — `chapter_generate` 工具失败不重置章节状态；`withTimeout` 非 AbortController 中断式。
- `server/src/routes/agents.ts:51-53` — `POST /api/agents` 不写 `is_custom=1`，用户 agent 不可删。
- `electron/main.ts:58-70` ✅ — `wipe-data`：`rmSync` 后 `app.exit(0)` 不先 kill server；`app.exit` 不触发 `before-quit` → Windows db 被锁 EBUSY 静默失败 + 成功路径孤儿进程。
- `electron/main.ts:371-377` — `safeStorage` 不可用回退明文（违反 #6）。
- `client/src/App.tsx:131` ✅ — CNY fetch 路径双 `/api` + 无 token + 无超时，静默吞错。
- `client/src/pages/SettingsPage.tsx:290-313` ✅ — `<p>`/`<div>` 在 `<select>` 内，无效 HTML。
- `client/src/pages/BookAnalysisPage.tsx`（原 AnalysisPanel 路径待定位）— `mergeBusy` 的 `finally` 调错 setter，按钮首次后永久禁用；写入前重新 grep 定位真实文件。
- `client/src/pages/AgentsLibraryPage.tsx:37,122` — 技能附件状态不从服务端初始化，恒显「未附加」。

## MEDIUM 级（~30 条，报告内逐条列明）主要类别
- 安全：`main.ts` 无 CSP + 破坏性 IPC 可被 XSS 触达；`security.ts` null-origin 仅在 `SERVER_TOKEN` 存在时校验；`keyCrypto.ts` 非 utility 模式明文回退；`restore-backup` 800ms 启发式竞态；`auto-backup` 不等 `checkpoint-done` 且只复制主库无 wal/shm。
- 正确性：`main.ts` server 异常退出不清 `lastServerUrl`、`before-quit` 无 `server.close()`；`ChapterExecutionPage` `withBusy` TOCTOU（state 非 ref）、focus Esc 提示不符、`produceWithSolution` 无本地错误处理、资源树无键盘可达性、快捷键 effect 每键重注册；`VolumePanel`/`AgentPanel`/`StylePanel`/`SetupPanel`/`StoryModesPage` 多处缺 busy 锁/disabled；`DirectorPage` 双重 confirm；`PromptWorkbenchPage` 还原不 invalidate 缓存；`BaseCharactersPage` 渲染期 setState；`MarketPanel` fetch 无超时。
- 类型/约束合规：`shared/types.ts`+`settings.ts`/`chapters.ts`/`solutions.ts` REST 泄漏 snake_case（#20）；`novels/volumes/worlds.ts` planner prompt/parse 重复内联（#31）；`seed.ts` 6 个 reserved model_route 未消费且 `reserved` 未落库（#30）；`shared/types.ts` 注释乱码（#22）；`.npmrc` 机器相关 `store-dir` 被 git 跟踪。

## LOW 级（~25 条）
非空断言/`as never`、`mainWindow!`、migrate 顺序、LIKE 通配未转义、`window.confirm` 与 `useConfirm` 并存、索引 key、无 aria 等，报告内汇总列出。

## 不做的事
- 不改任何源码/配置（用户选「仅出报告」）。
- 不跑 `pnpm typecheck/lint/dist`（无代码变更）。
- 不动仓库内任何文件（报告独立放桌面）。